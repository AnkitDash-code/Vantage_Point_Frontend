# Precomputed Mode Configuration
# Set to "true" to use precomputed static JSON files (no backend needed)
# Set to "false" or remove to use live backend API
NEXT_PUBLIC_USE_PRECOMPUTED=true

# Base URL for precomputed data files (relative to public folder)
NEXT_PUBLIC_PRECOMPUTED_BASE_URL=/precomputed

# Backend API URL (only used when USE_PRECOMPUTED is false)
NEXT_PUBLIC_API_URL=http://localhost:8000
